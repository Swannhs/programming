---
title: Write a C/C++/Java program If a five-digit number is input through the keyboard, write a program to calculate the sum of its digits. (Hint: Use the modulus operator ‘%’) (without loop)
category: Basic Declaration & Expression Index
tags: ['basic']
date: 2020-04-16
thumbnailText: Index
---

## C Implementation
### Program-27: If a five-digit number is input through the keyboard, write a C program to calculate the sum of its digits. (Hint: Use the modulus operator ‘%’) (without loop).
```c
/*If a five-digit number is input through the keyboard, write a C program to calculate the sum of its digits. (Hint: Use the modulus operator ‘%’) (without loop)*/
#include<stdio.h>
void main()
{
    int n,r=0,a;
    scanf("%d",&a);
    n=a%10;
    r=r+n;
    a=a/10;
    n=a%10;
    r=r+n;
    a=a/10;
    n=a%10;
    r=r+n;
    a=a/10;
    n=a%10;
    r=r+n;
    a=a/10;
    n=a%10;
    r=r+n;
    printf("Sum of Digit=%d",r);
}
```
### Output
```
34567
Sum of Digit:=25
```

## C++ Implementation
### Program-27: If a five-digit number is input through the keyboard, write a C++ program to calculate the sum of its digits. (Hint: Use the modulus operator ‘%’) (without loop).
```c
/*If a five-digit number is input through the keyboard, write a C++ program to calculate the sum of its digits. (Hint: Use the modulus operator ‘%’) (without loop)*/
#include<iostream>
using namespace std;
int main()
{
    int n,r=0,a;
    cin>>a;
    n=a%10;
    r=r+n;
    a=a/10;
    n=a%10;
    r=r+n;
    a=a/10;
    n=a%10;
    r=r+n;
    a=a/10;
    n=a%10;
    r=r+n;
    a=a/10;
    n=a%10;
    r=r+n;
    cout<<"Sum of Digit"<<r;
}
```
### Output
```
34567
Sum of Digit:=25
```

## Java Implementation
### Program-27: If a five-digit number is input through the keyboard, write a java program to calculate the sum of its digits. (Hint: Use the modulus operator ‘%’) (without loop).
```js
/*If a five-digit number is input through the keyboard, write a java program to calculate the sum of its digits. (Hint: Use the modulus operator ‘%’) (without loop)*/
import java.util.Scanner;
public class Funycode {
    public static void main(String[] args) {

        Scanner z=new Scanner(System.in);
        int n,r=0,a;
        a=z.nextInt();
        n=a%10;
        r=r+n;
        a=a/10;
        n=a%10;
        r=r+n;
        a=a/10;
        n=a%10;
        r=r+n;
        a=a/10;
        n=a%10;
        r=r+n;
        a=a/10;
        n=a%10;
        r=r+n;
        System.out.println("Sum of Digit:="+r);

    }
}
```
### Output
```
34567
Sum of Digit:=25
```
